# Production base URL for Vite (must end with /). Used by `vite build` and CI.
# Use `/` when the site is served at your domain root (custom domain on GitHub Pages, etc.).
# For default project Pages only — https://<user>.github.io/<repo>/ — set to /milynotes/ (repo name).
VITE_BASE_PATH=/milynotes/
